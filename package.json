{
  "name": "athletics-data-interface",
  "version": "1.0.0",
  "description": "Web interface for athletics governing body to access competition data",
  "scripts": {
    "dev": "concurrently \"npm run client\" \"cd backend && python app.py\"",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install && cd ../backend && pip install -r requirements.txt",
    "backend": "cd backend && python app.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["athletics", "database", "interface", "sports"],
  "author": "Athletics Governing Body",
  "license": "MIT"
}
